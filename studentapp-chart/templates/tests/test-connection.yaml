apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "studentapp-chart.fullname" . }}-test-connection"
  labels:
    {{- include "studentapp-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "studentapp-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
